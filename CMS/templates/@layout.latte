<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
        <title>{control menu:title|striptags}</title>
        {control resources:styles}
    </head>

    <body class="{$presenter->getName()|webalize} {$presenter->getAction()}">
        {include #layout}
        <footer>
            <hr>
            <div class="container">
                <div class="row">
                    <div class="col-xs-12">
                        <p class="text-right">
                            <strong>{_site.title}</strong> - {_site.copyright}
                        </p>
                    </div>
                </div>
            </div>
            <hr>
        </footer>
        {control resources:scripts}
    </body>
</html>